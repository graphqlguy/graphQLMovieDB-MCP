# Rebuild progress journal

class 2 done: recommendMoviesForMood + summarizeMovieReviews schema/Java, ReviewRepository derived query; compile OK
class 3 done: agent-grade schema descriptions (Movie rewrite, rating->averageRating with @deprecated + @SchemaMapping alias, Mood/WatchStatus/Sentiment per-value, addToWatchlist contract), three-state summary service, SchemaDescriptionLintTest with shrinking legacy allowlist; full test suite green
class 4 done: apollo/ directory (mcp-config.yaml, three operation files, schema symlink); mcp_class_4b points here (ch04b defines no concrete config)
class 5 done: apollo/ extended (custom-scalars.json, forward_headers dev shortcut, per-operation leading-comment descriptions, mcp-config-support.yaml Contracts example)
class 6 done: spring-ai-bom 2.0.0 + spring-ai-starter-mcp-server-webmvc, application.yaml SYNC/STREAMABLE + capabilities block, HelloMcpTool ping smoke test; compile OK
class 7 done: MovieOperations constants + MovieMcpTools with three @McpTool methods (records nested; fail-fast executeOperation); NOTE Boot 4 = Jackson 3, ObjectMapper imports are tools.jackson.* not com.fasterxml.*; compile + context boot OK
class 8 done: SchemaDescriptionProvider + SchemaDescriptionToolSpecifications (SyncMcpToolProvider rerun, Tool rebuilt with schema description), annotation-scanner disabled; boot shows the single expected ping fallback warning
class 9 done: ToolResults (structuredContent + text compat + isError, classification promoted), addToWatchlist returns CallToolResult via executeOperationLenient; reads stay typed (deviation from ch09 last paragraph, required by ch10-12/15 listings); boot OK
class 10 done: summarizeMovieReviews signature (context, @McpProgressToken, movieId) with three increasing progress checkpoints around the GraphQL execution; loadReviews/summarize helpers (chapter calls them illustrative) land in class 11; boot OK
class 11 done: WatchStatus elicitation on addToWatchlist (resolveStatus, WatchStatusRequest), sampling-or-fallback summarizer (loadReviews via MOVIE_REVIEWS operation, buildSummarizationPrompt/parseSummary, ServerSideSummarizer); boot OK
class 15 done: OAuth2 resource-server second chain for /mcp (McpSecurityConfig @Order(1) securityMatcher + RFC9728 WWW-Authenticate entrypoint, ProtectedResourceMetadataController, spring.security.oauth2 config w/ lazy jwk-set-uri so boot needs no network, moviedb.mcp.resource); @PreAuthorize SCOPE_ checks on the three tools. DEVIATION/fix: @PreAuthorize proxies MovieMcpTools (CGLIB) so SyncMcpToolProvider's getClass().getDeclaredMethods() scan lost the @McpTool methods -> subclassed the provider to scan AopUtils.getTargetClass() while invoking through the proxy (Method.invoke dispatches virtually, so @PreAuthorize still fires). Tests: added McpSecurityTestConfig (test JwtDecoder accepting any token w/ all scopes; Boot's decoder is @ConditionalOnMissingBean so it backs off) + httpRequestCustomizer Authorization header on the two boot MCP tests (2.0.0 SDK API, chapter's customizeRequest doesn't exist). Full suite 37 green
class 13 done: DIY MCP module in diymcp/ (DiyMcpController JSON-RPC dispatch on /diy-mcp/message with Origin validation + 202 for notifications + protocolVersion 2025-11-25, ToolRegistry, DiyMcpToolConfiguration @PostConstruct registers all three ops incl. summarize+addToWatchlist which the chapter elides as "analogous"); chapter's com.fasterxml.jackson imports adapted to tools.jackson to match the Boot4/Jackson3 injected ObjectMapper bean; compile OK + context loads
class 17 done: production observability. ToolMetricsAspect (@Around @McpTool -> mcp.tool.calls counter + mcp.tool.duration timer tagged tool/outcome, sits beside Class 16 AuditAspect on same pointcut); CostMetrics component (recordLlmCall -> mcp.llm.tokens/mcp.llm.calls, standalone per chapter, unwired). pom adds spring-boot-starter-actuator, micrometer-registry-prometheus, micrometer-tracing-bridge-otel, opentelemetry-exporter-otlp, net.logstash.logback:logstash-logback-encoder:9.0. application.yaml management block: actuator exposure health/info/prometheus, percentiles-histogram for mcp.tool.duration, tracing sampling 1.0. logback-spring.xml: LogstashEncoder JSON on root + separate audit.mcp logger (additivity false) - chose the XML customization path over the one-line logging.structured.format property because it is the concrete artifact routing Class 16's audit stream; the two are mutually exclusive at runtime so only the XML is present. application-production.yaml: SYNC/STREAMABLE, oauth2 issuer/audience from MCP_JWT_ISSUER/MCP_JWT_AUDIENCE env, actuator health show-details never, OTLP endpoint otel-collector:4318. DEVIATION: OTLP tracing endpoint lives in application-production.yaml only (not default application.yaml as the chapter titles it) so the test/default profile ships no collector address and boots offline. Full suite 37 green, console now emits Logstash JSON.
class 16 done: agent-specific security. SafeMutationTools stage-and-confirm (stageRemoveFromWatchlist + confirmAction, principal-bound single-use 60s token, @PreAuthorize SCOPE_watchlist:write on both, @Scheduled sweep, executeStaged runs the real REMOVE_FROM_WATCHLIST mutation in-process through ExecutionGraphQlService so ownership guards below MCP apply); SessionRateLimitInterceptor (bucket4j token bucket keyed principal[:sessionId], /mcp + /mcp/**, 429); AuditAspect (@Around @McpTool, audit.mcp logger); McpWebConfig registers interceptor + @EnableScheduling. pom adds spring-boot-starter-aspectj (Boot4 rename of starter-aop) + com.bucket4j:bucket4j_jdk17-core:8.14.0. SchemaDescriptionToolSpecifications adds safeMutationTools to provider. Contract snapshot regenerated (6 tools incl. stageRemoveFromWatchlist/confirmAction) via SnapshotWriter then deleted. Deviations from ch16 code block: AuditAspect ObjectMapper is tools.jackson (Boot4/Jackson3); SafeMutationTools drops the unused ObjectMapper ctor param and wires ExecutionGraphQlService for the real mutation; stageRemoveFromWatchlist carries destructiveHint annotations per chapter prose; bucket4j artifact is bucket4j_jdk17-core (current coordinates) not the doc's generic name. Full suite 37 green
class 12 done: four-level test suite in mcp/ (MovieMcpToolsUnitTest, McpEndpointIntegrationTest, ToolSchemaContractTest + contracts/tools-list.json snapshot, MoodToolPropertyTest, SummarizeProgressTest, MockGraphQlServiceFactory helper); prior agent's drafts kept, fixed SummarizeProgressTest progress() overload ambiguity with a Consumer<ProgressSpec> type witness; chapter's com.fasterxml.jackson imports adapted to tools.jackson (Boot4/Jackson3); snapshot generated via throwaway writer then removed; full suite 37 green
